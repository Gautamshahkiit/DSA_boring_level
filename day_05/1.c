// 1. create a singly linked list and display the same
//     i. insert a new node at beginning
//     ii. insert a node at the end of the list
//     iii. insert a node at any postion of the list

#include <stdio.h>

typedef struct linked_list{
  int data;
  struct linked_list *next;
}ll;

int main(){
  ll *head = NULL;
  ll *sec = NULL;
  ll *third = NULL;

  return 0;
}

// still doing